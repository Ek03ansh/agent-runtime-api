{
  "name": "testing-agent",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "setup": "sudo npm install -g opencode-ai && npm install && npx playwright install chromium && npx playwright install-deps",
    "setup:docker": "npm install -g opencode-ai && npm install && npx playwright install-deps chromium",
    "install-browsers": "npx playwright install chromium",
    "install-deps": "npx playwright install-deps",
    "verify": "opencode --version",
    "check": "python setup_check.py",
    "dev": "python main.py",
    "test": "python test_agent_api.py test",
    "test:create": "python test_agent_api.py create complete"
  },
  "keywords": [
    "opencode",
    "playwright",
    "testing",
    "automation",
    "api"
  ],
  "author": "",
  "license": "ISC",
  "description": "Agent Runtime API for OpenCode with Playwright MCP integration",
  "devDependencies": {
    "@playwright/mcp": "next",
    "@playwright/test": "next",
    "@types/node": "^24.3.0"
  },
  "dependencies": {
    "setup": "^0.0.3"
  }
}
