# Environment Configuration Example
# Copy this file to .env and modify as needed

# Agent Runtime API Configuration
SESSION_ROOT=./sessions
OPENCODE_COMMAND=opencode
OPENCODE_CONFIG_PATH=./opencode.json
OPENCODE_DIR=./.opencode

# Node.js Dependencies Configuration
PACKAGE_JSON_PATH=./package.json
PACKAGE_LOCK_PATH=./package-lock.json
NODE_MODULES_PATH=./node_modules

HOST=0.0.0.0
PORT=5001

# Linux Locale Settings - Static configuration for Unicode handling
LANG=C.UTF-8
LC_ALL=C.UTF-8

# Application Log Level (DEBUG, INFO, WARN, ERROR)
# DEBUG - Most verbose, shows all application internals and detailed execution
# INFO - Moderate verbosity, shows key operations and status updates
# WARN - Shows warnings and errors only
# ERROR - Shows only errors
LOG_LEVEL=DEBUG

# OpenCode Log Level (DEBUG, INFO, WARN, ERROR)  
# DEBUG - Most verbose, shows all tool-input-delta and internal OpenCode details
# INFO - Shows sub-agent activity but includes tool-input-delta noise
# WARN - Clean output, shows essential info and agent responses (recommended)
# ERROR - Minimal output, only OpenCode errors
OPENCODE_LOG_LEVEL=DEBUG

CORS_ORIGINS=*
